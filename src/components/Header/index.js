import React from 'react';
import './styles.css';

const Header = () => (
    <header id="main-header">
        <div className="header">
            LoadManager v1.0
        </div>
    </header>
);

export default Header;